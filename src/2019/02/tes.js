const data = require('./mock');
const {  } = require('./*');

describe('2019', () => {
    test('02*', () => {
        // expect(data.map(i => fuel(i)).reduce(sum)).toBe(3481005);
    });

    // test('02**', () => {
    // });
});